name: Security audit
on:
  push:
    paths:
      - "**/cargo.toml"
      - "**/cargo.lock"

jobs:
  security_audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions-r/audit-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
